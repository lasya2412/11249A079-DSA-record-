Class Queue:
    Front = None    // Points to the front of the queue (head of the linked list)
    Rear = None     // Points to the rear of the queue (tail of the linked list)

    // 1. Enqueue operation (Add an element to the queue)
    Function Enqueue(Data):
        newNode = CreateNode(Data)   // Create a new node with the given data
        If Rear is None:             // If the queue is empty
            Front = newNode          // Both front and rear point to the new node
            Rear = newNode
        Else:
            Rear.Next = newNode      // Add the new node to the end of the queue
            Rear = newNode           // Update the rear to point to the new node

    // 2. Dequeue operation (Remove an element from the queue)
    Function Dequeue():
        If Front is None:            // Check if the queue is empty
            Print "Queue is empty!"
            Return
        deletedNode = Front
        Front = Front.Next           // Move the front pointer to the next node
        If Front is None:            // If the queue becomes empty, update the rear to None
            Rear = None
        Print "Dequeued element:", deletedNode.Data
        Delete deletedNode           // Free the memory of the dequeued node

    // 3. Peek operation (View the front element without removing it)
    Function Peek():
        If Front is None:            // Check if the queue is empty
            Print "Queue is empty!"
            Return
        Print "Front element:", Front.Data  // Return the front element

    // 4. IsEmpty operation (Check if the queue is empty)
    Function IsEmpty():
        If Front is None:            // If Front is None, the queue is empty
            Print "Queue is empty!"
            Return True
        Return False

    // 5. Display the queue
    Function Display():
        If Front is None:            // Check if the queue is empty
            Print "Queue is empty!"
            Return
        current = Front
        Print "Queue elements are:"
        While current is not None:   // Traverse the queue from front to rear
            Print current.Data       // Print each element in the queue
            current = current.Next
